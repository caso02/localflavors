rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // No client-side Firestore access needed for MVP
    // All data flows through Cloud Functions
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
